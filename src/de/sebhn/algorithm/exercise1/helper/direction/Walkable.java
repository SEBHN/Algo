package de.sebhn.algorithm.exercise1.helper.direction;

public interface Walkable {

  // Position getNewPosition(Position currentPosition, Grid grid);

  // boolean isAllowedToWalk(Position newPosition);

}
